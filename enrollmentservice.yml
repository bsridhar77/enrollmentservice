template: Enrollment  is  %s
# use the simple server factory if you only want to run on a single port
server:
  # type: simple
  applicationConnectors:
    - type: http
      port: 5080
  adminConnectors:
    - type: http
      port: 5081